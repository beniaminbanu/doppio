<input 
    type="{{$text}}" 
    id="{{$id}}" 
    name="{{$name}}" 
    class="input {{$class}}" 
    placeholder="{{$placeholder}}" 
    min="{{$min}}" 
    max="{{$max}}"
    onkeypress="{{$keypress}}"
>